<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="8dp"
    android:paddingBottom="12dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/part1">
        <ImageView
            android:id="@+id/scholar_image"
            android:background="#FFFFFF"
            android:layout_width="120dp"
            android:layout_height="120dp" />
        <TextView
            android:id="@+id/scholar_name"
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:textColor="@color/colorUnread"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toRightOf="@id/scholar_image"
            android:textSize="20sp" />
        <TextView
            android:id="@+id/scholar_position"
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/colorUnread"
            android:textSize="13sp"
            app:layout_constraintLeft_toRightOf="@id/scholar_image"
            app:layout_constraintTop_toBottomOf="@id/scholar_name" />
        <TextView
            android:id="@+id/scholar_affiliation"
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/colorUnread"
            android:textSize="13sp"
            app:layout_constraintLeft_toRightOf="@id/scholar_image"
            app:layout_constraintTop_toBottomOf="@id/scholar_position" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.constraintlayout.widget.ConstraintLayout>